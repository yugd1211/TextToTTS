name: Build TTS from Content

on:
  push:
    paths:
      - 'content/**/*.md'
  workflow_dispatch:
    inputs:
      content_file:
        description: 'content/*.md file to convert'
        required: true
        type: string

permissions:
  contents: write

jobs:
  tts:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps
        run: pip install -r requirements.txt
      - name: Resolve target files
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            printf '%s\n' '${{ github.event.inputs.content_file }}' > /tmp/targets.txt
          else
            git diff --name-only ${{ github.event.before }} ${{ github.sha }} | grep '^content/.*\.md$' > /tmp/targets.txt || true
          fi
          cat /tmp/targets.txt
      - name: Generate audio (Edge TTS)
        env:
          EDGE_TTS_VOICE: ko-KR-SunHiNeural
          EDGE_TTS_RATE: +0%
          EDGE_TTS_PITCH: +0Hz
        run: |
          while IFS= read -r f; do
            [ -z "$f" ] && continue
            [ -f "$f" ] || continue
            python scripts/generate_tts.py "$f"
          done < /tmp/targets.txt
      - name: Commit audio
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add audio/
          if git diff --cached --quiet; then
            echo "No audio changes"
            exit 0
          fi
          git commit -m "chore(tts): generate audio with edge-tts"
          git push
